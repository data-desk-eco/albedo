etl:
  outputs:
    dev:
      type: duckdb
      path: ../data/data.duckdb
      threads: 1
      extensions:
        - spatial
        - httpfs
        - json
      settings:
        preserve_insertion_order: false
        temp_directory: ../data/temp
  target: dev
